# CPU works, but for GPU speed install CUDA PyTorch first, e.g.:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
ultralytics>=8.0.0
opencv-python>=4.8.0
torch>=2.0.0
torchvision>=0.15.0
